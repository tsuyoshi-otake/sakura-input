# Goal: Shift+Latin backspace/retype character-order is specified, tested, modeled, and residual-risked
Workdir: C:\Codes\tsuyoshi-otake\sakura-input

## Criteria
- C1: Independent domain oracle exists (no production algorithm import) and concrete example tests include the reported AIUEO→AIUOEO class plus passing/failing neighbors. Verify: `crates/sakura-engine/src/shift_latin_oracle.rs` exists and does not `use crate::dispatch` / `use crate::session`; `rtk cargo test -p sakura-engine --lib -- shift_latin_oracle` exits 0; test names/assertions in `shift_latin_oracle_tests.rs` encode the AIUEO family as oracle-expected sequences. Expect: at least the reported family is encoded as oracle-expected sequences.
- C2: PBT exists; seed and shrunk counterexample artifacts are written to disk under verification/. Verify: `verification/shift-latin-order/pbt-seed.txt` and `verification/shift-latin-order/pbt-shrunk-counterexample.md` exist and contain a numeric seed plus a human-readable event list (or an explicit no-fail record for that seed); `rtk cargo test -p sakura-engine --lib -- shift_latin_order` exits 0.
- C3: Production domain logic is fixed so oracle examples + PBT pass; character order after Shift-Latin, backspace-in-composition, and retype matches the oracle. Verify: `rtk cargo test -p sakura-engine --lib -- shift_latin` exits 0 and a named production test asserts the AIUEO repair sequence is not `AIUOEO`.
- C4: Atomic-condition C2 coverage report for the domain module is generated and stored. Verify: `verification/shift-latin-order/coverage/c2-report.md` exists and records per-condition coverage for the targeted oracle/production predicates (not workspace-wide line coverage claimed as MC/DC).
- C5: Mutation analysis artifact exists with score, surviving mutants, and equivalent-mutant classification. Verify: `verification/shift-latin-order/mutation-report.md` exists with those three sections and numeric killed/survived/timeout counts.
- C6: Persistence/external boundary inventory + protocol-compatible contract/API tests + failure-injection cases exist OR a written justification that this path is in-memory-only, plus still-required IPC/journal boundaries if keys cross engine. Verify: `verification/shift-latin-order/boundary-inventory.md` exists; `rtk cargo test -p sakura-engine --lib -- shift_latin_order::contract` exits 0.
- C7: TLA+ spec + TLC configs run; a verification record lists TLC version, settings, seed, generated/distinct states, diameter, constraints, unexplored scope, Safety/Liveness/deadlock/reachability results for ≥2 constant configurations. Verify: `verification/tla/ShiftLatinInput.tla`, at least two `.cfg` files, `verification/shift-latin-order/tla-record.md`, and saved TLC logs exist with real state counts.
- C8: Correspondence table + adversarial re-audit + residual-risk / release decision exist and a canvas visualizes them. Verify: `verification/shift-latin-order/correspondence-and-audit.md` and `C:\Users\developer\.cursor\projects\c-Codes-tsuyoshi-otake-sakura-input\canvases\shift-latin-order-verification.canvas.tsx` exist and are populated with real numbers/paths from this run (no placeholders).
- C9: TSF/engine-boundary Shift+Latin + Shift+Backspace + retype is covered by automated tests that do not reinstall the IME. Verify: `rtk cargo test -p sakura-tsf --lib -- shift_latin_then_shift_backspace_roundtrip_keeps_press_order` and `rtk cargo test -p sakura-tsf --lib -- shift_latin_backspace_retype_plans_commit_in_order_and_never_aiuoeo` both exit 0.
- C10: Conversion provenance is inside the shift_latin suite: named convert-then-backspace-then-retype neighbors of AIUEO/AIUOEO plus a convert-cancel-then-edit PBT with persisted seed/shrink, and an explicit resync-required conversion test. Verify: `rtk cargo test -p sakura-engine --lib -- shift_latin` exits 0; `verification/shift-latin-order/pbt-convert-seed.txt` and `pbt-convert-shrunk-counterexample.md` exist; test names include `resync_is_required_for_shifted_ascii_dictionary_conversion` and `production_convert_then_backspace_then_retype_keeps_aiueo_press_order`.
- C11: TLC has at least one finished configuration that increases MaxLen or MaxEvents beyond the previous passing pair (5/8 and 4/7), with recorded generated/distinct/diameter. Verify: `verification/shift-latin-order/tla-record.md` lists a completed medium or events config with real state counts and no timeout for that row; matching stdout.log exists under `verification/shift-latin-order/tlc/`.
- C12: Mutation report includes a cargo-mutants scoped run (not workspace-wide) and classifies S1 as killed by the shift_latin suite, with numeric killed/survived/timeout/equivalent counts. Verify: `verification/shift-latin-order/mutation-report.md` names `cargo-mutants`, records a score, and does not list S1 as a campaign survivor.
- C13: A real llvm-cov (or repo-equivalent) artifact exists for the production functions named in the residual-risk list, labeled as line/region coverage and not as MC/DC. Verify: `verification/shift-latin-order/coverage/llvm-cov-report.md` exists, contains numeric coverage for `feed_character`, `apply_backspace`, `render_preedit`, and `resync_shifted_ascii_from_raw`, and explicitly says it is not C2/MC/DC.
- C14: Shift-Latin-relevant arm region coverage is measured separately from kana/CJK and is ≥70% for `feed_character`, `apply_backspace`, and `render_preedit`. Verify: `verification/shift-latin-order/coverage/llvm-cov-report.md` has a Shift-Latin-relevant arm table whose three named functions each show an arm region percent ≥ 70, and an Out of scope table that names the kana/pending-romaji line ranges. Expect: no silent hole; MC/DC is still labeled unavailable.
- C15: A stronger-than-fake-pipe TSF/HWND layer exists and passes, and the audit does not call a lookable gap “irreducible.” Verify: `rtk cargo test -p sakura-tsf --lib -- shift_latin_settext_payloads_reach_a_process_local_edit_hwnd_and_never_aiuoeo` and `rtk cargo test -p sakura-tsf --lib -- shift_latin_backspace_retype_plans_chain_to_aiueo_not_aiuoeo` both exit 0; `verification/shift-latin-order/correspondence-and-audit.md` Residual risk item 1 names the process-local EDIT HWND and states that a live `ITfContext`/`ITfRange` and the installed-IME `e2e-host` path remain the only physical impossibilities. Expect: installer/reinstall was not required for these tests.
