# Goal: Developer-history hot enable/disable is specified, tested, modeled, and residual-risked
Workdir: /workspace

## Criteria
- C1: Independent domain oracle exists (no production algorithm import) and concrete example tests include the observed stale-inactive class (setting ON, published, request after publish, service not attached) as forbidden. Verify: `crates/sakura-engine/src/developer_history_oracle.rs` exists and does not `use crate::dispatch` / `use crate::session` / `use crate::input_history`; `developer_history_oracle_tests.rs` encodes the forbidden stale-inactive family. Expect: oracle forbids `published_on ∧ request_after_publish ∧ ¬service_attached`.
- C2: PBT exists; seed and shrunk counterexample artifacts are written under verification/developer-history/. Verify: `verification/developer-history/pbt-seed.txt` and `verification/developer-history/pbt-shrunk-counterexample.md` exist with a numeric seed plus a human-readable event list or an explicit no-fail record; targeted oracle/PBT tests exit 0 on a Windows host (or compile-check on CI Windows).
- C3: Production hot-enable matches the oracle: after config publish of developer-mode ON, the next request boundary attaches history and `InputHistoryStats.active` is true without engine restart; OFF detaches. Verify: server/dispatch/settings tests covering attach/detach exit 0; CLI no longer treats ON+live+inactive as a normal terminal success.
- C4: Atomic-condition C2 coverage report for the domain module is generated and stored. Verify: `verification/developer-history/coverage/c2-report.md` exists and records per-condition polarity coverage for the oracle predicates (not claimed as MC/DC).
- C5: Mutation analysis artifact exists with score, surviving mutants, and equivalent-mutant classification. Verify: `verification/developer-history/mutation-report.md` exists with those three sections.
- C6: Persistence/external boundary inventory + protocol-compatible contract/API tests + failure-injection cases exist. Verify: `verification/developer-history/boundary-inventory.md` exists; pipe or dispatcher contract tests cover Stats/Flush/Clear and Normal-scope recording after hot-enable.
- C7: TLA+ spec + TLC configs run; a verification record lists TLC version, settings, seed, generated/distinct states, diameter, constraints, unexplored scope, Safety/Liveness/deadlock/reachability for ≥3 constant configurations. Verify: `verification/tla/DeveloperHistory.tla`, at least three `.cfg` files, `verification/developer-history/tla-record.md`, and saved TLC logs under `verification/developer-history/tlc/` exist with real state counts.
- C8: Correspondence table + adversarial re-audit + residual-risk / release decision exist and a canvas visualizes them. Verify: `verification/developer-history/correspondence-and-audit.md` and a `developer-history-verification.canvas.tsx` under the workspace canvases directory exist and are populated with real numbers/paths from this run (no placeholders).
