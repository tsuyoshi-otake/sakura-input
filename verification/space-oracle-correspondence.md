# Space oracle and adapter correspondence (#106)

Program baseline: `f48d8f55fcab3f9abb210aa2a442cd8b217df1f1`.
Patch base: `bc90f95` (PR #136). Classification: **CONFIRMED verification-code
defects**, not a real TSF product failure. #106 and the full program remain open.

Three counterexamples failed before correction, each with cargo exit 101 and
one semantic assertion failure:

| Verify | Expect | Before | Correction |
|---|---|---|---|
| `live_peer_absorption_preserves_the_pending_teardown_credit` | A live peer owns the first absorption; after it commits, the lost reading still owns one Space | Oracle spent the credit early | `apply_space` spends only when no live peer owns the absorption |
| `disconnect_of_a_live_reading_owes_one_space_to_a_surviving_idle_peer` | The surviving idle peer absorbs one Space, then inserts on the next; dead target ignores Space | Oracle inserted immediately | Disconnect of a live reading arms credit |
| `production_disconnect_releases_the_live_claim_and_keeps_one_teardown_credit` | Adapter models actual dispatcher teardown, allowing the second Space through | Adapter retained the live claim indefinitely | Adapter resets its dispatcher before stopping delivery |

The first fixture contains two accepted readings at the abstract contract
boundary. It is not evidence that serial raw Type keys can establish both in
the engine. The actual fence's `consume_teardown` already refuses consumption
while another live claim exists; the engine implementation is unchanged.
The server worker calls `dispatcher.reset()` at connection end. Merely setting
the adapter's `live` flag did not exercise that terminal action.

The oracle imports no dispatcher, fence or production decision helper. Its
state transitions continue to derive expectations from domain events. The
production adapter exercises the real dispatcher/fence, without real pipes,
COM, process death or physical key routing. `AbandonContext` replaces the
ambiguous `ReplaceContext` event name. `CrashRestart` explicitly means a
connection reset with a surviving engine fence. Orderly replacement after
settlement remains excluded from teardown credit.

Verification uses `cargo test --locked -p sakura-engine --lib
space_key_dispatch`: 47 passed, 0 failed, 0 ignored after correction. This
includes the oracle's deterministic xorshift campaign, seed
`0x5350_4143_4520_0816`, 512 cases with 1–3 actors and 3–10 events, plus explicit
boundary probes. `coverage/c2-report.md` reports 11 named oracle atoms with
22/22 polarity observations. This is neither production condition coverage nor
MC/DC. The independent shrink canary reaches `[7, 3]`; it is a test of a known
synthetic predicate, not a shrunk current product failure.

The file named `pbt-production-shrunk-counterexample.md` is generated by a
**fixed two-event regression**, `Type(C0), Space(C0|C1)`. It has no randomized
production search or current failing shrink. Its legacy seed label must not
be counted as an independent production PBT campaign. The new result manifest
classifies that test according to what actually runs.

Mutation is scoped to `space_key_dispatch_oracle.rs` and tests selected by
`space_key_dispatch_oracle`. It is not a dispatch mutation score. The config's
old claim that production dual delivery is a known baseline failure has been
removed; it was a historical assertion rather than an exclusion justified by
this run. Generated/caught/missed/unviable/timeout outcomes are recorded from
cargo-mutants 27.1.0, with a real baseline, one concurrent job and bounded
build/test/outer process lifetimes in a copied tree under `~/tmp/`. Actual result:
33 generated, 32 caught, 1 unviable, 0 missed/timeout/error. Every caught outcome
has a successful build followed by test exit 101 and Rust assertion failures.
The unviable `space_effect -> Default::default()` mutation fails with E0277
because `SpaceEffect` has no `Default`; it is not a semantic kill. The baseline
ran 24 selected oracle tests successfully. Local process inventories were clean.

Final workspace verification: 1,821 passed / 84 ignored / 0 failed. Ignored tests
were not executed. Formatting, all-target Clippy with warnings denied, and diff
checks passed. The independent reviewer passed the correction; its initial
wording “33 caught” was corrected against the actual tool outcomes above.

Remaining: add teardown credit and non-vacuous checks to the TLA model, bind
current campaign dependencies and artifacts to CI freshness checks, and qualify
real TSF/physical routing separately. Boolean credit coalescing, broader
multi-connection equivalence and whole-engine death are not established by
these three regressions. Local raw logs remain under
`C:/Users/developer/tmp/sakura-revalidation-evidence-20260905`; committed results
carry their hashes. Independent review is behaviorally read-only, not an OS
sandbox; executable verification belongs to the parent.
