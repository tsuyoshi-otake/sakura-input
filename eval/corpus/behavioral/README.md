# Behavioral corpus

These cases are deterministic contracts. They are never sent to Luna Max.

| Family | Owns |
|--------|------|
| `composition/` | composition lifecycle, commit count, exact literal preservation in the FSM |
| `key-routing/` | `consumed`, Space dual-delivery, `TestKeyDown → KeyDown` |
| `candidate-ui/` | popup ownership, stale popup, renderer provenance |
| `timeout/` | IPC timeout, stale revision |

Issue #67 TSF Contract E2E lands here as executable fixtures, not as Judge prompts.
