# Issue完了判定レビュー（2026-08-08）

このファイルは、GitHub Issueの「完了判定」監査に関する引き継ぎメモです。`CLAUDE.md`（VS Codeクラッシュ調査の引き継ぎ）とは別の作業スレッドの記録として、意図的に別ファイルに分けています。`#7`は両ファイルに関係します。

## 実施内容

1. `gh issue list` でopen 16件を確認し、コメント履歴とcommit metadataから「実質完了・クローズ待ち」の候補として `#7`・`#10`・`#14`・`#15` を判定。
2. リポジトリを **PUBLIC** に変更（2026-08-08、ユーザーが実施）。
3. ChatGPT Pro（GitHub連携で実コード・実issueを直接確認、思考時間約29分）にこの完了判定をアドバーサリアルにレビューさせた。依頼文の原本はローカル一時ファイル（`~/tmp/review-sakura-input-completion.md`、リポジトリ外）。
4. レビュー結果を元に、`#7`・`#10`・`#14`・`#15` の各Issueへ検証コマンド付きのコメントを追加済み。以下はその要約。詳細は各Issueのコメントを参照。

## ChatGPT Proの結論（要検証・鵜呑み厳禁）

**4件とも「実質完了」としてのクローズは不当**という評価。`~/.claude/notes/chatgpt-pro-adversarial-review.md` の方針どおり、以下はまだローカルで再現・確認していない外部AIの指摘であり、次のエージェントが最初に行うべきは検証であって、記述の適用ではありません。

- **`#7`**（VS Codeクラッシュ対策。`CLAUDE.md`の「最優先タスク」と同一テーマ）：防御的hardening実装は完了した可能性が高いが、VS Codeクラッシュの解消自体は未証明、という指摘。`c7813d9`は修正差分ではなく既存未追跡ファイルのbaseline化コミットである可能性が指摘されており、もしそうなら第三者が実際の修正内容をdiffから再構成できない。
  **`CLAUDE.md`の「最優先タスク」セクションが前提とする『2026-08-02時点でまだ仮説段階』という記述は、その後同日中に実装が進み"ship"判定まで到達していたことがissueコメントから確認できている。しかし今回の独立レビューにより、その"ship"判定自体の実ホスト裏付けが不足していると再指摘された。** 次のエージェントは「未着手」ではなく「実装はあるが実ホスト検証が欠けている」現在地から再開すべき。
- **`#10`**（開発者向け入力履歴モード）：コードは存在するが、Issue自体に受入証拠が一切ない。`#15`が実ホストで全入力破棄という重大な後発欠陥を発見しており、"コード存在"≠"機能完成"の実例になっている。
- **`#14`**（post-install dogfood）：成功根拠が`#15`によって反証済み（"queue drops 0"等は実は全入力破棄の誤検知だった）。加えて、real-pipeテストがインストール済みengineをwell-known pipe経由で再利用し、実ユーザーのlearning/historyストアへ書き込みうる、というテスト隔離上の重大な懸念が新たに指摘されている。**これは`#14`単体の問題ではなく、リポジトリのテストスイート全体の信頼性に関わる可能性がある。**
- **`#15`**：Issue本文の受入条件（短単語top-1改善）を満たさず、コメント自身が6件の未対応readingを明記。実learning replayでtop-1が悪化する問題も未対応。

詳細な再判定理由・具体的な検証コマンド（`cargo test`の個別テスト名、DLLロード証明、`Get-WinEvent`によるクラッシュ収集、実プロファイルmanifest diff、corpus-eval再測定など）は、各Issueに追加したコメントを参照してください。

## 次のエージェントへの推奨着手順

1. **最優先**：`#14`のテスト隔離問題の実在確認。インストール済みengineが起動した状態で、外側からLOCALAPPDATAを隔離パスに変更し `pipe_round_trip` の実engineテストを実行、実プロファイル（`%LOCALAPPDATA%\SakuraInput`）のmanifest（再帰的ファイル一覧＋SHA-256）が変化するかを確認する。変化するなら、テストスイート全体の隔離設計を先に直さないと、以降のどの「642 passed」等の報告も実ユーザーデータへの書き込みを否定できない。
2. `#7`：Code.exe/notepad.exeが実際にロードしている`sakura_tsf.dll`のSHA-256が、installer報告・registryのInprocServer32と一致するかを確認する。
3. `#15`：corpus-evalで、かんじ／いこう／かいぎょう／しゃさい／してき／ほしい の6件を実際に再測定し、修正前後で改善しているかを確認する。
4. 上記いずれも、プレイブックの方針（外部AIの指摘は鵜呑みにせずローカルで再現してから動く）に従うこと。

## 関連ファイル・リンク

- `CLAUDE.md` — VS Codeクラッシュ調査（`#7`関連）の別スレッド記録。本ファイルとは独立に管理し、本セッションでは変更していない。
- レビュー依頼文の原本（ローカル一時ファイル、リポジトリ外）：`~/tmp/review-sakura-input-completion.md`
- ChatGPT Proとの会話: `https://chatgpt.com/c/6a771c99-c2b8-83ee-8ab3-81c20a9a78f6`
